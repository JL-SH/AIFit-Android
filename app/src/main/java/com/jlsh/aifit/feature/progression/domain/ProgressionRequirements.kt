package com.jlsh.aifit.feature.progression.domain

/** Minimum logged sessions required before the app may request a progression recommendation. */
object ProgressionRequirements {
    const val MIN_SESSIONS_REQUIRED = 3
}
